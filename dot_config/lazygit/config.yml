os:
  editCommand: "nvim"
  editCommandTemplate: "{{editor}} +{{line}} -- {{filename}}"
gui:
  nerdFontsVersion: "3"
  showIcons: true
git:
  diffRenderers:
    - colorArg: always
      command: diff-so-fancy
